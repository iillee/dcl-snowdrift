node_modules/
bin/
dist/
.parcel-cache/
*.log

# Secrets (Discord webhooks, API keys, etc.) — loaded via @dcl/sdk/server EnvVar
.env
.env.*

# Agent skill packs (installed by tooling, not part of the scene)
.claude/
agent/
thumbnails/
assets/images/screenshot/

# Source art files (Blender, SketchUp) and their backups
*.blend
*.blend[0-9]*
*.skp
*.skb
*.bak
*~
